{
  "id": "1c3415988ab06726b54583f03a7185c059dc9c07e348c64e32ad8555e6022a84",
  "full_path": "do time-sheet-cost-code-allocations update",
  "description": "Update a time sheet cost code allocation.",
  "permissions": "User must be an admin or a member of the customer associated with the time sheet customer.",
  "side_effects": "After commit, updates related project_phase_cost_item_actuals for the allocation.",
  "validation_notes": "The <id> argument is required. At least one updatable attribute must be provided (currently --details). If --details is provided, it must be a JSON array of objects and include at least one allocation; each allocation may only include cost_code_id, percentage, and optional project_cost_classification_id; percentage must be between 0 and 1; total allocation percentage must equal 1.0 (100%); cost_code_id values must belong to the customer; duplicate cost_code_id values are not allowed.",
  "flags": [
    {
      "name": "--details",
      "aliases": null,
      "required": false,
      "type": "string",
      "description": "Allocation details JSON array",
      "default": null,
      "validation": "JSON array of objects; must include at least one allocation"
    }
  ],
  "sources": [
    {
      "repo_name": "cli",
      "file_path": "internal/cli/do_time_sheet_cost_code_allocations_update.go"
    },
    {
      "repo_name": "server",
      "file_path": "app/models/concerns/cost_code_allocation.rb"
    },
    {
      "repo_name": "server",
      "file_path": "app/policies/concerns/cost_code_allocation_policy.rb"
    }
  ]
}
